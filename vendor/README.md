# Frozen compatibility artifact

`credo-6f4f57c.tar` is the immutable CREDO `3.0.0a5` source archive built from
commit `6f4f57c114606476cb6e93c6d52dc34628173a4d`. It is a compatibility and
provenance dependency, not editable source and not imported by v4 numerical
internals.

Verify with:

```bash
sha256sum --check SHA256SUMS
```
